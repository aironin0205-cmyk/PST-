{
  "name": "pst-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Professional English to Persian Translation Platform",
  "scripts": {
    "dev:backend": "pnpm --filter backend dev",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
