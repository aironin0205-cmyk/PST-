{
  "name": "pst-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Professional English to Persian Translation Platform",
  "scripts": {
    "dev:backend": "pnpm --filter backend dev",
    "build": "pnpm -r build",
    "graph": "nx graph",
    "lint": "nx run-many --target=lint",
    "typecheck": "nx run-many --target=typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "nx": "^19.8.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
